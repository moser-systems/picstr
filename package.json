{
  "name": "picstr",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end assets for picstr",
  "scripts": {
    "build": "node build.mjs",
    "install-and-build": "npm install && npm run build"
  },
  "dependencies": {
    "@tabler/core": "^1.3.2",
    "hyperscript.org": "^0.9.91",
    "leaflet": "^1.9.4",
    "tom-select": "^2.6.0"
  }
}
